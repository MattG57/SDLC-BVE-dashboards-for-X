{
  "name": "bve-shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared modules for BVE dashboard pipeline — math, format, config, source processing",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "stream-json": "^2.1.0"
  }
}
